name = "helioflux-api-proxy"
main = "workers/api-proxy.js"
compatibility_date = "2024-02-10"

# Account ID (optional but recommended)
# Get your account ID from: https://dash.cloudflare.com/
# account_id = "your-account-id-here"

# Optional: Custom domain routing for production
# Uncomment and configure after setting up Cloudflare Pages
# [env.production]
# routes = [
#   { pattern = "your-domain.com/api/*", zone_name = "your-domain.com" }
# ]

# Worker configuration
# For more configuration options, see:
# https://developers.cloudflare.com/workers/configuration/
